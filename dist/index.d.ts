
// FILE GENERATED BY `rollup-plugin-dts@0.14.0`
// https://github.com/Swatinem/rollup-plugin-dts

interface IVType {
    iv: any;
}
interface ICipher {
    setKey(key: string): void;
    getKey(): string;
    toString(decrypted: any): string;
    decrypt(data: any): string;
}

declare class Cipher implements ICipher {
    private key;
    constructor();
    static getIV(encrypted: IVType): IVType;
    setKey(key: string): void;
    getKey(): string;
    toString(decrypted: any): string;
    decrypt(data: any): string;
}

export { Cipher };
